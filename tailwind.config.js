/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html", // Include the main HTML file if using Vite or a similar setup
    "./src/**/*.{js,ts,jsx,tsx}", // Cover all JavaScript and TypeScript files in the `src` directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
